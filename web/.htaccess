# Postavke i log obrasca nisu za preglednik.
<FilesMatch "^(mail-config\.php|mail-test\.log|build\.ps1)$">
    Require all denied
</FilesMatch>

# Izvorni dijelovi stranica (src/) — radi i kad je site u podmapi (npr. /new/).
RedirectMatch 404 /src/
